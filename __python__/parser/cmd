sudo docker compose  up --build      - запуск с выводом в консоль
sudo docker compose up --build -d    - запуск в удалённом режиме (на фон)

sudo docker compose logs -f          - проверка вывода в реальном времени в консоль после включения удалённого режима спустя какое-то время(час,2 часа, день)